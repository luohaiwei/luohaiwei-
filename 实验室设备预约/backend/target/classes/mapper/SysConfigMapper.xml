<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab.reservation.mapper.SysConfigMapper">

    <select id="selectByGroup" resultType="com.lab.reservation.entity.SysConfig">
        SELECT * FROM sys_config
        WHERE config_group = #{group}
        ORDER BY id
    </select>

    <select id="selectByKey" resultType="com.lab.reservation.entity.SysConfig">
        SELECT * FROM sys_config
        WHERE config_key = #{configKey}
        LIMIT 1
    </select>

</mapper>
