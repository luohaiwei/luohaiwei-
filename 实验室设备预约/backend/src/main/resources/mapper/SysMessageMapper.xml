<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab.reservation.mapper.SysMessageMapper">

    <!-- 统计用户未读消息数量 -->
    <select id="countUnreadByUserId" resultType="int">
        SELECT COUNT(*) FROM sys_message
        WHERE user_id = #{userId} AND is_read = 0 AND deleted = 0
    </select>

    <!-- 将用户所有消息标记为已读 -->
    <update id="markAllAsReadByUserId">
        UPDATE sys_message
        SET is_read = 1, update_time = NOW()
        WHERE user_id = #{userId} AND is_read = 0 AND deleted = 0
    </update>

</mapper>
